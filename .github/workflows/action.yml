on:
  push:
    branches:
      - main

jobs:
  run_pull:
    name: run pull
    runs-on: ubuntu-latest
    steps:
    - name: change
      uses: omeryttnc/SchoolProjectAPI_typescript@main
      with:
        root_file: package.json
        working-directory: SchoolProjectAPI_typescript
    - name: install
      run: |
        npm install
  
